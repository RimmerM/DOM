import { render } from "./dom";
import { createClass, createElement } from "./react";
import { newNode, newTextNode } from "./node";

export { render, newNode, newTextNode, createClass, createElement };
